# Optional TimescaleDB stack, distinct from the root SQLite demo.
# Set unique random secrets; compose refuses blank required values.
JWT_SECRET_KEY=
POSTGRES_PASSWORD=
ADMIN_USERNAME=
ADMIN_PASSWORD=
EDGE_API_KEY=
